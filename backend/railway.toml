[build]
builder = "nixpacks"

[deploy]
startCommand = "php artisan serve --host=0.0.0.0 --port=$PORT"
healthcheckPath = "/api/auctions/1"
healthcheckTimeout = 10

[[services]]
name = "bidding-backend"
